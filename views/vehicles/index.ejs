<%- include('../partials/html-head') %>
<%- include('../partials/nav') %>

<section>
  <h1>Cars and Motorcycles!!! 🏎️ 🏍️</h1>
  <% if (user) { %>
    <h2>Add a Vehicle! 🏎️ 🏍️</h2>
    <form action="/vehicles" method="POST">
      <label>
        Year: 
        <input type="text" name="year" autocomplete="off">
      </label>
        Make: 
        <input type="text" name="make" autocomplete="off">
      </label>
        Model: 
        <input type="text" name="model" autocomplete="off">
      </label>
        Color: 
        <input type="text" name="color" autocomplete="off">
      </label>
      Type:
      <select name="type">
        <option value="car">Car 🏎️ </option>
        <option value="motorcycle">Motorcylce 🏍️</option>
      </select>
      <label>
        Fast 💯:
        <input type="checkbox" name="fast">
      </label>
      <button type="submit">Add Vehicle</button>
    </form>

  <% } %>
  <h2>Recent Vehicle Searches Below! 🏎️ 🏍️</h2>
	<ul>
  <% vehicles.forEach(vehicle => { %>
    <li> 
      <%= vehicle.year %> 
      <%= vehicle.make %> 
      <%= vehicle.model %> color
      <%= vehicle.color %> is
      <%= vehicle.fast ? 'fast' : 'not fast' %></li>
  <% }) %>
  </ul>
</section>

<%- include('../partials/footer') %>